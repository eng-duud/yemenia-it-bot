# 🎓 بوت دليل الطالب الجامعي (النسخة الاحترافية)

بوت تليجرام متكامل مصمم بلغة **Python** لخدمة الطلاب الجامعيين، مع لوحة تحكم كاملة للأدمن وجاهزية تامة للنشر على منصات الاستضافة.

## 🌟 المميزات المحدثة
- **نظام أدمن متكامل:** إضافة المواد والمحتوى (PDF وروابط) مباشرة من داخل البوت.
- **دعم الاستضافة:** يتضمن ملفات `Procfile` و `Dockerfile` للنشر السريع.
- **إدارة ذكية للبيانات:** استخدام SQLite مع هيكلية جداول مترابطة.
- **تجربة مستخدم محسنة:** أزرار تنقل واضحة ومعالجة شاملة للأخطاء.

## 🚀 تعليمات النشر على الاستضافة

### الخيار الأول: VPS (Ubuntu/Debian)
1. قم بتثبيت Python و Git:
   ```bash
   sudo apt update && sudo apt install python3-pip git -y
   ```
2. ارفع ملفات المشروع إلى السيرفر.
3. ثبت المكتبات:
   ```bash
   pip install -r requirements.txt
   ```
4. استخدم `screen` أو `systemd` لتشغيل البوت في الخلفية:
   ```bash
   python3 main.py
   ```

### الخيار الثاني: Docker (الخيار الموصى به)
1. قم ببناء الحاوية:
   ```bash
   docker build -t student-bot .
   ```
2. تشغيل الحاوية:
   ```bash
   docker run -d --name my-student-bot student-bot
   ```

## ⚙️ الإعدادات الأساسية (config.py)
تأكد من ضبط القيم التالية قبل التشغيل:
- `BOT_TOKEN`: التوكن من @BotFather.
- `ADMIN_ID`: معرفك الشخصي لتفعيل لوحة التحكم.
- `REQUIRED_CHANNEL`: يوزر القناة للتحقق من الاشتراك.

## 📂 هيكلية المشروع
- `main.py`: نقطة الدخول الرئيسية.
- `handlers/`: تحتوي على منطق العمل للمستخدم والأدمن.
- `keyboards/`: تصميم واجهات الأزرار.
- `data/`: إدارة قاعدة البيانات.
- `Dockerfile` & `Procfile`: ملفات التكوين للاستضافة.

---
**تم إعداد هذا المشروع ليكون جاهزاً للعمل الفوري بمجرد إدخال التوكن.**
